{
  // WHY: In Lambda/ECS there is no ~/.aws/credentials file.
  // We null out Profile so the SDK skips the shared-file provider
  // and falls through to the execution role / task role.
  "Aws": {
    "Region": "ap-south-1",
    "AccountId": "123456789012",
    "Profile": null
  }
}